sudo: required

language: ruby

services:
  - docker

script: docker-compose up -d postgres && docker-compose run --rm api rake

before_script:
  - docker-compose run --rm api bundle exec rails db:migrate